rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // User Vaults: Scoped per user key for cross-platform device sync
    match /vaults/{userKey} {
      allow read, write: if true;
    }
    match /users/{userId} {
      allow read, write: if true;
    }
  }
}
